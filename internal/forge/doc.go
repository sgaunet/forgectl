// Package forge is the platform abstraction. It owns the Forge interface, the
// domain types both implementations speak, the shared HTTP transport carrying the
// timeout and the bounded retry, and the sentinel errors callers classify on.
package forge
