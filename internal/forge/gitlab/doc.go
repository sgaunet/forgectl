// Package gitlab implements forge.Forge against the GitLab REST API through the
// official client, and adds the project access token lifecycle, which has no
// equivalent on the other platform.
package gitlab
